// Function to handle redirection
function handleRedirect(selectedValue) {
    if (selectedValue) { // Check if a non-empty value is selected
      window.location.href = selectedValue; // Redirect to the selected URL
    }
  }